const form=document.querySelector('#sub-service-form'),title=document.querySelector('#sub-service-form-title');function field(name){return form.querySelector(`[name="${name}"]`)}function resetSubService(){if(!form)return;form.reset();field('id').value='';field('active').checked=true;field('sort_order').value='0';title.textContent='Tambah sublayanan'}
document.querySelectorAll('.edit-sub-service').forEach(button=>button.addEventListener('click',()=>{const item=JSON.parse(button.dataset.item);field('id').value=item.id;field('service_id').value=item.service_id;field('name').value=item.name;field('description').value=item.description||'';field('sort_order').value=item.sort_order;field('active').checked=item.active;title.textContent='Edit sublayanan';form.scrollIntoView({behavior:'smooth',block:'start'})}));document.querySelector('#sub-service-reset')?.addEventListener('click',resetSubService);
