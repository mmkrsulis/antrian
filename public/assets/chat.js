(()=>{const launcher=document.querySelector('#internal-chat-launcher'),panel=document.querySelector('#internal-chat-panel');if(!launcher||!panel||!window.INTERNAL_CHAT)return;const close=document.querySelector('#internal-chat-close'),messages=document.querySelector('#internal-chat-messages'),form=document.querySelector('#internal-chat-form'),input=document.querySelector('#internal-chat-input'),badge=document.querySelector('#internal-chat-badge'),status=document.querySelector('#internal-chat-status');let cursor=0,unread=0,timer=null,loading=false,initialized=false;
const escRole=role=>({super_admin:'Super Admin',admin:'Admin',operator:'Operator'}[role]||role);const time=value=>{const date=new Date(String(value).replace(' ','T'));return Number.isNaN(date.getTime())?'':date.toLocaleTimeString('id-ID',{hour:'2-digit',minute:'2-digit'})};
function setBadge(value){unread=Math.max(0,Number(value)||0);badge.hidden=!unread;badge.textContent=unread>99?'99+':String(unread)}function setStatus(text,error=false){status.textContent=text;status.classList.toggle('is-error',error)}
function addMessage(item){const empty=messages.querySelector('.internal-chat-empty');empty?.remove();const article=document.createElement('article');article.className='internal-chat-message'+(Number(item.user_id)===Number(INTERNAL_CHAT.userId)?' is-own':'');article.dataset.id=item.id;const body=document.createElement('div');body.textContent=item.body;const footer=document.createElement('footer'),name=document.createElement('b'),meta=document.createElement('span');name.textContent=item.sender_name;meta.textContent=`${escRole(item.sender_role)} · ${time(item.created_at)}`;footer.append(name,meta);article.append(body,footer);messages.append(article);cursor=Math.max(cursor,Number(item.id)||0)}
async function markRead(){if(!cursor)return;setBadge(0);try{await fetch('/api/chat/read',{method:'POST',headers:{'Content-Type':'application/json','X-CSRF-Token':INTERNAL_CHAT.csrf},body:JSON.stringify({last_id:cursor})})}catch{}}
async function poll(){if(loading)return;loading=true;try{const response=await fetch(`/api/chat/messages?after=${cursor}`,{cache:'no-store'});if(!response.ok)throw new Error(`HTTP ${response.status}`);const data=await response.json(),items=data.messages||[];if(!initialized&&!items.length)messages.innerHTML='<p class="internal-chat-empty">Belum ada pesan. Mulai percakapan dengan operator lain.</p>';items.forEach(addMessage);if(items.length)messages.scrollTop=messages.scrollHeight;initialized=true;setStatus('Terhubung');if(panel.hidden)setBadge(data.unread);else await markRead()}catch(error){setStatus('Koneksi chat terputus · mencoba kembali',true)}finally{loading=false;schedule()}}
function schedule(){clearTimeout(timer);timer=setTimeout(poll,panel.hidden?8000:2500)}function openPanel(){panel.hidden=false;launcher.setAttribute('aria-expanded','true');input.focus();messages.scrollTop=messages.scrollHeight;markRead();schedule()}function closePanel(){panel.hidden=true;launcher.setAttribute('aria-expanded','false');launcher.focus();schedule()}
launcher.addEventListener('click',()=>panel.hidden?openPanel():closePanel());close.addEventListener('click',closePanel);addEventListener('keydown',event=>{if(event.key==='Escape'&&!panel.hidden)closePanel()});input.addEventListener('input',()=>{input.style.height='auto';input.style.height=Math.min(input.scrollHeight,110)+'px'});input.addEventListener('keydown',event=>{if(event.key==='Enter'&&!event.shiftKey){event.preventDefault();form.requestSubmit()}});
form.addEventListener('submit',async event=>{event.preventDefault();const body=input.value.trim();if(!body)return;const button=form.querySelector('button');button.disabled=true;try{const response=await fetch('/api/chat/messages',{method:'POST',headers:{'Content-Type':'application/json','X-CSRF-Token':INTERNAL_CHAT.csrf},body:JSON.stringify({body})});const data=await response.json();if(!response.ok)throw new Error(data.error||`HTTP ${response.status}`);input.value='';input.style.height='auto';if(data.message&&!messages.querySelector(`[data-id="${data.message.id}"]`))addMessage(data.message);messages.scrollTop=messages.scrollHeight;setStatus('Terkirim');await markRead()}catch(error){setStatus(error.message||'Pesan gagal dikirim',true)}finally{button.disabled=false;input.focus()}});poll()})();
