document.querySelector('#admin-display-settings')?.addEventListener('submit',async event=>{event.preventDefault();const form=event.currentTarget,status=document.querySelector('#admin-media-status'),button=form.querySelector('button[type="submit"]');button.disabled=true;status.textContent='Uploading and saving media settings…';try{const response=await fetch('/api/admin/display-settings',{method:'POST',headers:{'X-CSRF-Token':ADMIN_CSRF},body:new FormData(form)});const body=await response.json();if(!response.ok)throw new Error(body.error);status.textContent='Display media saved. The display updates automatically.';setTimeout(()=>location.reload(),800)}catch(error){status.textContent=error.message}finally{button.disabled=false}});
document.querySelector('#header-height-settings')?.addEventListener('submit',async event=>{event.preventDefault();const form=event.currentTarget,status=document.querySelector('#header-height-status'),data=new FormData(form);status.textContent='Saving header height…';try{const response=await fetch('/api/admin/header-height',{method:'POST',headers:{'Content-Type':'application/json','X-CSRF-Token':ADMIN_CSRF},body:JSON.stringify({_csrf:ADMIN_CSRF,mode:data.get('mode'),height:Number(data.get('height'))})});const body=await response.json();if(!response.ok)throw new Error(body.error);status.textContent='Header height saved. Refresh open screens to apply it.'}catch(error){status.textContent=error.message}});
